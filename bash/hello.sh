#!/usr/bin/env bash

# disk_percent=$(command df / | tail -1 | awk '{print $5}' | tr -d '%')
# disk_percent=67
# if [ "$disk_percent" -ge 90 ]; then
#   echo "CRITICAL: disk usage is ${disk_percent}%"
# elif [ "$disk_percent"  -ge 80 ]; then
#   echo "WARNING: disk usage is ${disk_percent}%"
#
# else 
#   echo "OK: disk usage is ${disk_percent}%"
# fi
#



