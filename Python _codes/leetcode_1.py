# Make a function which will sort the brackets (open and close) and close their 
# Make a function which will understand what brackets is next

s = "(}"

myList = ['(', ')', '{', '}', '[', ']']


def firstStep():
    for i in range(len(s) - 1):
        a = s[i]
        a1 = s[i + 1]
        for j in range(len(myList) - 1 )
        if
            return s[i], s[i +1 ]
        else:
            return False

print(firstStep())
